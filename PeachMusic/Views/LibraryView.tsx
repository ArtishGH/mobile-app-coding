import React from 'react';
import { View, Text, FlatList, Image, TouchableOpacity } from 'react-native';
import { Song, data } from './Song';

export const LibraryView = () => {
  return (
    <View>
    </View>
  );
};
